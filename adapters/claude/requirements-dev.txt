-r requirements.txt

# Dev/test only. The contract test (tests/test_schema_contract.py) also runs
# standalone with the stdlib (`python tests/test_schema_contract.py`); pytest is
# only needed for the `pytest` runner / CI integration.
pytest>=8.0.0

# Editable install of the deterministic IR compiler, so `import pycompiler` resolves
# in IDEs/type checkers too (the adapter itself falls back to a sys.path insert when
# the package isn't installed — see ir_execution_port.py).
-e ../../compiler/solidworks
